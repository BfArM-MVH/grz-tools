#!/bin/bash
set -euo pipefail

# run the 'pending' rule, passing all args to snakemake
snakemake --profile /etc/snakemake/container-profile --snakefile /grz-watchdog/workflow/Snakefile --directory /workdir pending "$@"
