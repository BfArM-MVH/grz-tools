monitor:
  interval: 5
batch:
  limit: 2

temp-outputs: false 
on-failed-validation: "cleanup" 
auto-cleanup: "inbox+storage"

config_paths:
  inbox:
    "123456789":
      test1: "inbox.test1.yaml"
  archive:
    consented: "archive.consented.yaml"
    nonconsented: "archive.nonconsented.yaml"
  db: "db.yaml"
  pruefbericht: "pruefbericht.yaml"

qc:
  revision: "v1.1.5"
