"""Canonical grz-cli/metadata version-compatibility policy, bundled with grz-common.

Kept in sync with the code that reads/writes ``VersionFile`` by shipping in the same
package release, rather than being fetched over the network at runtime.
"""
